table:
  schema: public
  name: category
is_enum: true
array_relationships:
  - name: quests
    using:
      foreign_key_constraint_on:
        column: category
        table:
          schema: public
          name: quests
